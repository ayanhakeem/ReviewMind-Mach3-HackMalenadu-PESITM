# ReviewMind Backend

This is the Node.js/Express server for ReviewMind. It handles product scraping and proxies AI requests.

### Setup
1. `npm install`
2. Create `.env` from `.env.example`
3. `node server.js`

For full project details, see the [Root README](../README.md).
